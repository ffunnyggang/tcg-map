/* FUNY PIN home banner carousel — local image registry */
(()=>{const BANNERS=[
{image:'assets/banners/2D2B760B-3151-49E8-8725-F803D112CD08.png',url:'https://m.blog.naver.com/ffunyggang/224344470429',active:true,order:1},
{image:'assets/banners/DFE8B3E3-B41F-4E48-8DBD-E7EFEFF3AD9C.png',url:'partner.html',active:true,order:2},
{image:'assets/banners/DFE8B3E3-B41F-4E48-8DBD-E7EFEFF3AD9C.png',url:'partner.html',active:true,order:3}
].filter(x=>x.active).sort((a,b)=>a.order-b.order);if(!BANNERS.length)return;const mapLink=[...document.querySelectorAll('a')].find(a=>(a.textContent||'').replace(/\s+/g,' ').trim().includes('TCG MAP에서 카드샵 찾아보기'));if(!mapLink)return;const anchor=mapLink.closest('.portal-more-row,.portal-embed-more')||mapLink.parentElement||mapLink;const root=document.createElement('section');root.className='home-banner';root.setAttribute('aria-label','추천 배너');root.innerHTML=`<div class="home-banner-viewport"><div class="home-banner-track">${BANNERS.map((b,i)=>`<article class="home-banner-slide"><a href="${b.url}" ${/^https?:/.test(b.url)?'target="_blank" rel="noopener noreferrer"':''} aria-label="배너 ${i+1} 열기"><img src="${b.image}" alt="" ${i?'loading="lazy"':'fetchpriority="high"'} draggable="false"></a></article>`).join('')}</div></div>${BANNERS.length>1?`<div class="home-banner-dots" aria-label="배너 페이지">${BANNERS.map((_,i)=>`<span role="button" tabindex="0" class="home-banner-dot${i===0?' is-active':''}" aria-label="배너 ${i+1} 보기"></span>`).join('')}</div>`:''}`;anchor.insertAdjacentElement('afterend',root);const viewport=root.querySelector('.home-banner-viewport'),track=root.querySelector('.home-banner-track'),dots=[...root.querySelectorAll('.home-banner-dot')];let index=0,timer=null,startX=0,startY=0,dx=0,dragging=false;const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;function show(n){index=(n+BANNERS.length)%BANNERS.length;track.style.transform=`translate3d(${-index*100}%,0,0)`;dots.forEach((d,i)=>d.classList.toggle('is-active',i===index))}function stop(){if(timer){clearInterval(timer);timer=null}}function start(){stop();if(BANNERS.length>1&&!reduce)timer=setInterval(()=>show(index+1),4500)}dots.forEach((d,i)=>{const go=()=>{show(i);start()};d.addEventListener('click',go);d.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();go()}})});viewport.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'&&e.button!==0)return;dragging=true;startX=e.clientX;startY=e.clientY;dx=0;stop()});viewport.addEventListener('pointermove',e=>{if(!dragging)return;dx=e.clientX-startX;const dy=e.clientY-startY;if(Math.abs(dx)>Math.abs(dy)+5)e.preventDefault()});const end=()=>{if(!dragging)return;dragging=false;if(Math.abs(dx)>42)show(index+(dx<0?1:-1));start()};viewport.addEventListener('pointerup',end);viewport.addEventListener('pointercancel',end);viewport.addEventListener('pointerleave',e=>{if(e.pointerType==='mouse')end()});document.addEventListener('visibilitychange',()=>document.hidden?stop():start());start()})();
