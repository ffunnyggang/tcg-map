import { useEffect } from 'react';import { ActivityIndicator,SafeAreaView } from 'react-native';import { useLocalSearchParams,useRouter } from 'expo-router';import { supabase } from '../../lib/supabase';export default function AuthCallback(){const p=useLocalSearchParams<{code?:string}>(),r=useRouter();useEffect(()=>{(async()=>{if(p.code)await supabase.auth.exchangeCodeForSession(p.code);r.replace('/account')})()},[p.code]);return <SafeAreaView style={{flex:1,justifyContent:'center'}}><ActivityIndicator/></SafeAreaView>}
