(()=>{const body=document.body,header=document.querySelector('.portal-header');if(!body||!header)return;body.classList.add('home-motion');let ticking=false;const update=()=>{ticking=false;const y=Math.max(0,window.scrollY||0);const start=6,end=92;const p=Math.min(1,Math.max(0,(y-start)/(end-start)));body.style.setProperty('--home-title-fade',p.toFixed(3));header.style.pointerEvents=p>.92?'none':''};const onScroll=()=>{if(!ticking){ticking=true;requestAnimationFrame(update)}};window.addEventListener('scroll',onScroll,{passive:true});window.addEventListener('resize',onScroll,{passive:true});update()})();